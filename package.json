{
  "name": "food-donation-app",
  "version": "1.0.0",
  "description": "MERN Stack Food Donation Web Application",
  "private": true,
  "scripts": {
    "dev": "echo 'Run from backend and frontend directories separately'",
    "build": "cd frontend && npm run build"
  }
}
